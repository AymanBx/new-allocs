// The header file heapSize.h, containing (besides the mandatory header guard) a single line: #define HEAPSIZE <number of bytes> is only #included in the
// source file replacementManager.cpp. You may modify the value assigned to the
// heap size, but cannot add any other line of code, and in particular any other macro definition
// (again, besides the header guard) to this file.

#ifndef HEAP_SIZE_H
#define HEAP_SIZE_H

#define HEAPSIZE	5000	// for example


#endif //	HEAP_SIZE_H
